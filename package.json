{
  "name": "wa-runtime",
  "version": "1.0.0",
  "description": "A full WhatsApp runtime with backend service and web dashboard",
  "type": "module",
  "devDependencies": {
    "@eslint/js": "^9.39.2",
    "@types/bun": "latest",
    "@types/ws": "^8.18.1",
    "eslint": "^9.39.2",
    "globals": "^16.5.0",
    "jiti": "^2.6.1",
    "prettier": "3.7.4",
    "typescript-eslint": "^8.50.1",
    "concurrently": "^9.1.2"
  },
  "peerDependencies": {
    "typescript": "latest"
  },
  "scripts": {
    "start": "bun run ./index.ts",
    "server": "bun run ./server.ts",
    "web": "cd astro-web-runtime && node ./dist/server/entry.mjs",
    "web:dev": "cd astro-web-runtime && npx astro dev",
    "web:build": "cd astro-web-runtime && npx astro build",
    "start:all": "concurrently --kill-others \"bun run server\" \"bun run web\"",
    "dev": "concurrently --kill-others \"bun run server\" \"bun run web:dev\"",
    "postinstall": "bun run hook.ts",
    "lint": "eslint . --ext .ts --fix",
    "format": "prettier --write ."
  },
  "dependencies": {
    "@realastrox11/bunql": "^1.2.0",
    "audio-decode": "^2.2.3",
    "baileys": "https://github.com/WhiskeySockets/Baileys",
    "libphonenumber-js": "^1.12.33",
    "pino-pretty": "^13.1.3"
  }
}
